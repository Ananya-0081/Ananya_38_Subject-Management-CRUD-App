# -Subject-Management-CRUD-App
Created a subject management system to manage subject code and subject name using REST APIs. Endpoints: POST /subjects GET /subjects PUT /subjects/{id} DELETE /subjects/{id}
